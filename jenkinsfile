//sample
pipeline {
  agent any
  
  stages {
    stage('Clone repository') {
      steps {
        checkout scm
        bat 'echo ${env.JENKINS_HOME} on ${env.JOB_URL}'
        bat 'echo $env.JOB_BASE_NAME'
      }
    }
}
}
