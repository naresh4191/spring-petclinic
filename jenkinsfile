//sample
pipeline {
  agent any
  
  stages {
    stage('Clone repository') {
      steps {
        checkout scm
        bat 'echo $env.JOB_NAME'
        bat 'echo $env.JOB_BASE_NAME'
      }
    }
}
}
